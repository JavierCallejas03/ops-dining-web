(function () { 'use strict'; var WEBHOOK_URL = 'https://n8n.opsdining.com/webhook/ops-dining-form'; var form = document.getElementById('secure-form'); var submitBtn = document.getElementById('submit-btn'); var statusMsg = document.getElementById('form-status'); if (!form) return; form.addEventListener('submit', async function (e) { e.preventDefault(); submitBtn.disabled = true; submitBtn.innerText = 'PROCESANDO...'; var formData = { nombre: form.querySelector('[name="nombre"]').value.trim(), restaurante: form.querySelector('[name="restaurante"]').value.trim(), email: form.querySelector('[name="email"]').value.trim(), telefono: form.querySelector('[name="telefono"]').value.trim(), timestamp: new Date().toISOString(), source: 'web-opsdining' }; try { var res = await fetch(WEBHOOK_URL, { method: 'POST', headers: { 'Content-Type': 'application/json', 'Accept': 'application/json' }, body: JSON.stringify(formData) }); if (res.ok) { showStatus('SOLICITUD ENVIADA CON ÉXITO. TE CONTACTAREMOS EN BREVE.', false); form.reset(); submitBtn.innerText = 'ENVIADO'; setTimeout(function() { if (window.closeModal) window.closeModal('contacto'); }, 3000); } else { throw new Error(); } } catch (error) { showStatus('ERROR AL ENVIAR. INTÉNTELO DE NUEVO.', true); submitBtn.disabled = false; submitBtn.innerText = 'REINTENTAR ENVÍO'; } }); function showStatus(message, isError) { if (!statusMsg) return; statusMsg.innerText = message; statusMsg.classList.remove('hidden'); if (isError) { statusMsg.className = 'mt-8 text-[9px] font-bold uppercase tracking-widest block p-4 border border-red-500/50 text-red-500 italic text-center bg-red-500/5'; } else { statusMsg.className = 'mt-8 text-[9px] font-bold uppercase tracking-widest block p-4 border border-[#c5a059]/50 text-[#c5a059] italic text-center bg-[#c5a059]/5'; } } })();
