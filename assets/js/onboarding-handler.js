document.addEventListener('DOMContentLoaded', function () { const form = document.getElementById('onboarding-form'); const steps = document.querySelectorAll('.step-content'); const nextBtn = document.getElementById('next-btn'); const prevBtn = document.getElementById('prev-btn'); const submitBtn = document.getElementById('submit-btn'); const progressFill = document.getElementById('progress-fill'); const stepLabel = document.getElementById('step-label'); const stepPercentage = document.getElementById('step-percentage'); const successMessage = document.getElementById('success-message'); let currentStep = 1; const WEBHOOK_URL = 'https://n8n.opsdining.com/webhook/ops-dining-onboarding'; function updateStep() { steps.forEach(step => { step.classList.toggle('active', parseInt(step.dataset.step) === currentStep); }); const progress = (currentStep / steps.length) * 100; progressFill.style.width = `${progress}%`; stepLabel.innerText = `Paso ${currentStep} de ${steps.length}`; stepPercentage.innerText = `${Math.round(progress)}%`; prevBtn.classList.toggle('invisible', currentStep === 1); if (currentStep === steps.length) { nextBtn.classList.add('hidden'); submitBtn.classList.remove('hidden'); } else { nextBtn.classList.remove('hidden'); submitBtn.classList.add('hidden'); } window.scrollTo({ top: 0, behavior: 'smooth' }); } function validateStep() { const activeStep = document.querySelector('.step-content.active'); const inputs = activeStep.querySelectorAll('input, select, textarea'); let isValid = true; inputs.forEach(input => { input.style.borderColor = 'rgba(255,255,255,0.1)'; const container = input.closest('div'); const oldError = container.querySelector('.error-msg'); if (oldError) oldError.remove(); let fieldValid = true; let errorText = ""; if (input.hasAttribute('required') && !input.value.trim()) { fieldValid = false; errorText = "Este campo es obligatorio"; } else if (input.hasAttribute('pattern') && input.value.trim()) { const regex = new RegExp("^" + input.getAttribute('pattern') + "$"); if (!regex.test(input.value.trim())) { fieldValid = false; errorText = input.title || "Formato incorrecto"; } } else if (input.type === 'email' && input.value.trim()) { if (!/^[^\s@]+@[^\s@]+\.[^\s@]+$/.test(input.value.trim())) { fieldValid = false; errorText = "Email inválido"; } } else if (input.type === 'url' && input.value.trim()) { try { new URL(input.value.trim()); } catch (_) { fieldValid = false; errorText = "URL inválida"; } } if (!fieldValid) { isValid = false; input.style.borderColor = '#ef4444'; const error = document.createElement('p'); error.className = 'error-msg text-[#ef4444] text-[10px] mt-1 italic animate-pulse'; error.innerText = errorText; container.appendChild(error); } }); if (!isValid) { const firstError = activeStep.querySelector('.error-msg'); if (firstError) firstError.scrollIntoView({ behavior: 'smooth', block: 'center' }); } return isValid; } nextBtn.addEventListener('click', function () { if (validateStep()) { currentStep++; updateStep(); } }); prevBtn.addEventListener('click', function () { if (currentStep > 1) { currentStep--; updateStep(); } }); form.addEventListener('submit', async function (e) { e.preventDefault(); if (!validateStep()) return; submitBtn.disabled = true; const originalText = submitBtn.innerText; submitBtn.innerText = 'Enviando...'; const formData = new FormData(form); const data = Object.fromEntries(formData.entries()); data.submittedAt = new Date().toISOString(); try { const response = await fetch(WEBHOOK_URL, { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify(data) }); if (response.ok) { successMessage.classList.remove('hidden'); document.body.style.overflow = 'hidden'; window.scrollTo(0, 0); } else { throw new Error(); } } catch (err) { alert('Error al enviar. Inténtalo de nuevo.'); submitBtn.disabled = false; submitBtn.innerText = originalText; } }); });
